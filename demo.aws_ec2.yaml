plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  tag:name: Nginx
  instance-state-name : running
hostnames:
  # you can also specify a list of in order of precedence for hostname variables.
  - private_ip_address